{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/phaseimpute/master/assets/schema_input_plink_text.json",
    "title": "nf-core/phaseimpute pipeline - params.input schema for PLINK text format",
    "description": "Schema for the PLINK text format input samplesheet file used by the phaseimpute pipeline",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^[a-zA-Z0-9_-]+$",
                "errorMessage": "Sample name must only contain alphanumeric characters, underscores, and hyphens. Spaces are not allowed.",
                "meta": ["id"]
            },
            "ped": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.ped$",
                "errorMessage": "PLINK text genotype file (.ped) must be provided and cannot contain spaces"
            },
            "map": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.map$",
                "errorMessage": "PLINK variant map file (.map) must be provided and cannot contain spaces"
            }
        },
        "required": ["sample", "ped", "map"]
    }
}
